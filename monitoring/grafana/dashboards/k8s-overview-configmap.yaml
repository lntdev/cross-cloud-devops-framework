apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboards-k8s
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  README.txt: |
    Drop JSON dashboards here as more keys if you like.

